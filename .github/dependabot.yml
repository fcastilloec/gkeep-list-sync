version: 2
updates:
  # Maintain dependencies for GitHub Actions
- package-ecosystem: github-actions
  directory: "/"
  commit-message:
    prefix: ci
  schedule:
    interval: weekly
    time: "18:45"
